server {
    listen 80;
    server_name petpelace.store www.petpelace.store;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    client_max_body_size 10M;
    server_name petpelace.store www.petpelace.store;

    ssl_certificate     /etc/letsencrypt/live/petpelace.store/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/petpelace.store/privkey.pem;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:80/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass http://backend:80/uploads/;
    }
}
